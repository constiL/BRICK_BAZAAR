<%= simple_form_for [@brick, @buy_request] do |f| %>
  <%= f.submit "Buy", class: "btn" %>
<% end %>


<%= simple_form_for [@brick, @buy_request] do |f| %>
  <%= f.input :accepted %>
  <%= f.submit "Buy", class: "btn" %>
<% end %>
