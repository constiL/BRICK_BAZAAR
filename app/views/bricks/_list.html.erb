<div data-search-bricks-target="list">
  <p class="h4">
    <% if params[:query].blank? %>
      <%= pluralize bricks.count, 'brick' %> available
    <% else %>
      <%= pluralize bricks.count, 'result' %> for "<%= params[:query] %>"
    <% end %>
  </p>

  <div class="bricks">
    <% bricks.each do |brick| %>
      <%= render "brick_infos", brick:  %>
    <% end %>
  </div>
</div>
