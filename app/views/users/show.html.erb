<% @bricks.each do |brick| %>
  <%= link_to brick_path(brick) do %>
    <%= brick.name %>
    <br>
  <% end %>
<% end %>
<br>
<% @buy_requests.each do |buy_request| %>
  <%= link_to brick_path(buy_request.brick) do %>
    <%= buy_request.brick.name %>
  <br>
  <% end %>
<% end %>

<% @buy_requests.each do |buy_request| %>
  <%= link_to "Cancel", buy_request_path(buy_request), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }  %>
<% end %>
